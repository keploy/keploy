# Force TLS + SCRAM for everything except the unix socket used by
# pg_isready and the entrypoint's init-db dance.
local   all             all                                     trust
hostssl all             all             0.0.0.0/0               scram-sha-256
hostssl all             all             ::/0                    scram-sha-256

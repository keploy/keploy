name: Keploy Performance Test

on:
  workflow_dispatch:
  pull_request:
    branches: [ main ]

jobs:
  performance-test:
    runs-on: ubuntu-latest
    
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: petclinic
          MYSQL_DATABASE: petclinic
        ports:
          - 3306:3306
        options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=5

    steps:
      - name: Checkout Keploy (current branch with scripts)
        uses: actions/checkout@v4
        with:
          path: keploy

      - name: Checkout Spring PetClinic
        uses: actions/checkout@v4
        with:
          repository: spring-projects/spring-petclinic
          path: petclinic

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'

      - name: Setup k6
        uses: grafana/setup-k6-action@v1

      - name: Build Spring PetClinic
        working-directory: petclinic
        run: ./mvnw clean package -DskipTests

      - name: Build Keploy
        working-directory: keploy
        run: |
          # Build from current branch (which has yaml-optimization changes)
          go build -o keploy main.go

      - name: Make scripts executable
        working-directory: keploy
        run: chmod +x scripts/performance-test/*.sh

      - name: Start PetClinic with Keploy recording
        working-directory: keploy
        run: ./scripts/performance-test/start-petclinic-with-keploy.sh

      - name: Create k6 load test script
        working-directory: keploy
        run: ./scripts/performance-test/create-k6-script.sh

      - name: Run k6 load test
        working-directory: keploy
        run: k6 run load-test.js 2>&1 | tee k6-output.log

      - name: Stop Keploy
        if: always()
        working-directory: keploy
        run: ./scripts/performance-test/stop-keploy.sh

      - name: Validate RPS threshold
        working-directory: keploy
        run: ./scripts/performance-test/validate-rps.sh

      - name: Display Keploy test cases
        if: always()
        working-directory: keploy
        run: ./scripts/performance-test/display-test-cases.sh

name: Golang On Docker (macOS)
on:
  workflow_call:

jobs:
  golang_docker_macos:
    concurrency:
      group: macos-keploy-${{ github.ref }}
      cancel-in-progress: false
    runs-on: [self-hosted, macOS, native]
    strategy:
      fail-fast: false
      matrix:
        app:
          - name: gin-mongo
            path: gin-mongo
            script_dir: gin_mongo
          - name: echo-sql
            path: echo-sql
            script_dir: echo_sql
        config:
          # - job: record_latest_replay_build
          #   record_src: latest
          #   replay_src: build
          - job: record_build_replay_latest
            record_src: build
            replay_src: latest
          # - job: record_build_replay_build
          #   record_src: build
          #   replay_src: build
    name: ${{ matrix.app.name }} (${{ matrix.config.job }}) (macOS)
    steps:

      - name: Clear docker images and cache
        run: |
          docker builder prune --all -f
          docker volume prune -f

      - name: Clean up git config
        if: always()
        continue-on-error: true
        run: |
          git config --global --unset url."git@github.com:".insteadOf

      - name: Checkout repository
        uses: actions/checkout@v4

      - id: image
        uses: ./.github/actions/download-image
        env:
          DOCKER_HOST: ""

      - id: record
        uses: ./.github/actions/download-binary
        with:
          src: ${{ matrix.config.record_src }}

      - id: replay
        uses: ./.github/actions/download-binary
        with:
          src: ${{ matrix.config.replay_src }}

      - name: Checkout the samples-go repository
        uses: actions/checkout@v4
        with:
          repository: keploy/samples-go
          path: samples-go

      - name: Run ${{ matrix.app.name }} application (macOS)
        env:
          RECORD_BIN: ${{ steps.record.outputs.path }}
          REPLAY_BIN: ${{ steps.replay.outputs.path }}
        shell: bash
        run: |
          cd samples-go/${{ matrix.app.path }}
          bash $GITHUB_WORKSPACE/.github/workflows/test_workflow_scripts/golang/${{ matrix.app.script_dir }}/golang-docker-macos.sh

      # - name: Final cleanup (macOS)
      #   if: always()
      #   env:
      #     RECORD_BIN: ${{ steps.record.outputs.path }}
      #     REPLAY_BIN: ${{ steps.replay.outputs.path }}
      #   shell: bash
      #   run: |
      #     set +e
      #     docker compose down || true
      #     docker ps -aq --filter "name=ginApp" | xargs -r docker rm -f || true
      #     docker ps -aq --filter "name=ginApp_test" | xargs -r docker rm -f || true
      #     docker ps -aq --filter "name=mongoDb" | xargs -r docker rm -f || true
      #     docker network rm keploy-network || true
      #     find "$GITHUB_WORKSPACE" -type d -name keploy -prune -exec rm -rf {} + || true
      #     rm -rf "$TMPDIR"/keploy-run-* || true
      #     rm -f "$RECORD_BIN" "$REPLAY_BIN" || true

      - name: Clean up git config
        if: always()
        continue-on-error: true
        run: |
          git config --global --unset url."git@github.com:".insteadOf
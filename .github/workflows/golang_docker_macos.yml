name: Golang On Docker (macOS)
on:
  workflow_call:
    inputs:
      image_tag:            
        description: "Docker image tag or digest to pull"
        required: false
        type: string
    secrets:
      MAC_RUNNER_USER_PASSWORD:
        description: "Keychain password for macOS runner"
        required: true

jobs:
  golang_docker_macos:

    runs-on: [self-hosted, macOS, native]
    strategy:
      fail-fast: false
      matrix:
        app:
          - name: echo-sql
            path: echo-sql
            script_dir: echo_sql
          - name: gin-mongo
            path: gin-mongo
            script_dir: gin_mongo
        config:
          - job: record_build_replay_build
            record_src: build
            replay_src: build
    name: ${{ matrix.app.name }} (${{ matrix.config.job }}) (macOS)

    steps:
      - name: Unlock keychain
        if: runner.os == 'macOS'
        run: |
          security unlock-keychain -p $KEYCHAIN_PASSWORD
        env:
          KEYCHAIN_PASSWORD: ${{ secrets.MAC_RUNNER_USER_PASSWORD }}

      - name: Clean up git config
        if: always()
        continue-on-error: true
        run: |
          rm -rf ~/.gitconfig

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Ensure Docker Desktop is running (macOS)
        shell: bash
        run: |
          set -e
          if docker info >/dev/null 2>&1; then
            echo "Docker daemon is already running."
          else
            echo "Docker daemon not responding. Restarting Docker Desktop..."
            # Start Docker Desktop detached from this process tree so the
            # GH Actions runner won't kill it as an "orphan" in post-job cleanup.
            nohup open -g -a Docker >/dev/null 2>&1 &
            disown
            for i in $(seq 1 90); do
              if docker info >/dev/null 2>&1; then
                echo "Docker Desktop is ready (waited ~$((i*2))s)."
                break
              fi
              if [ "$i" -eq 90 ]; then
                echo "ERROR: Docker Desktop failed to start within 180s."
                exit 1
              fi
              sleep 2
            done
          fi

      - id: image
        uses: ./.github/actions/download-image
        with:
          image_tag: ${{ inputs.image_tag }}
          version: ${{ github.sha }}

      - id: record
        uses: ./.github/actions/download-binary
        with:
          src: ${{ matrix.config.record_src }}

      - id: replay
        uses: ./.github/actions/download-binary
        with:
          src: ${{ matrix.config.replay_src }}

      - name: Checkout the samples-go repository
        uses: actions/checkout@v4
        with:
          repository: keploy/samples-go
          path: samples-go

      - name: Ensure Docker Compose is available (macOS)
        shell: bash
        run: |
          set -e
          if docker compose version >/dev/null 2>&1; then
            echo "Compose V2 plugin found."
            exit 0
          fi
          if command -v docker-compose >/dev/null 2>&1; then
            echo "docker-compose (v1) found."
            exit 0
          fi
          # Install docker-compose (v1) via Homebrew as a fallback
          brew update
          brew install docker-compose
          echo "Installed docker-compose."

      - name: Run ${{ matrix.app.name }} application (macOS)
        env:
          RECORD_BIN: ${{ steps.record.outputs.path }}
          REPLAY_BIN: ${{ steps.replay.outputs.path }}
          JOB_ID: ${{ github.run_id }}-${{ github.job }}-${{ github.run_attempt }}
        shell: bash
        run: |
          cd samples-go/${{ matrix.app.path }}
          bash $GITHUB_WORKSPACE/.github/workflows/test_workflow_scripts/golang/${{ matrix.app.script_dir }}/golang-docker-macos.sh

      - name: Kill the running script processes
        if: always()
        continue-on-error: true
        run: |
          pkill -f "bash $GITHUB_WORKSPACE/.github/workflows/test_workflow_scripts/golang/${{ matrix.app.script_dir }}/golang-docker-macos.sh" || true

      - name: Clean up Docker artifacts
        if: always()
        continue-on-error: true
        run: |
          for c in ginApp ginApp_1 ginApp_2 ginApp_test mongoDb; do
            docker rm -f "$c" 2>/dev/null || true
          done
          docker network rm keploy-network 2>/dev/null || true
          pkill -9 -f 'keploy' 2>/dev/null || true

      - name: Clean up git config
        if: always()
        continue-on-error: true
        run: |
          git config --global --unset-all url."git@github.com:".insteadOf 2>/dev/null || true
          git config --global --unset-all url."ssh://git@github.com/".insteadOf 2>/dev/null || true
          git config --global --unset-all core.sshCommand 2>/dev/null || true
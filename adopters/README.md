# Adopters

This directory contains adoption stories from organizations and individual users who are using Keploy in real-world environments.

- `organizations/` contains adoption entries from companies using Keploy
- `users/` contains adoption entries from individual developers

Each entry is added via a pull request.
